{
  "name": "tootycms-plugins",
  "private": true,
  "version": "0.0.0",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "validate": "node ./scripts/validate-plugins.mjs",
    "guard:auth": "node ./scripts/authorization-guard.mjs",
    "security:scan": "node ./scripts/security-scan.mjs",
    "ci": "npm run validate && npm run guard:auth && npm run security:scan"
  }
}
